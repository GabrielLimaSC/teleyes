export function PlaceholderPage({ title }: { title: string }) {
  return (
    <main style={{ padding: '32px 16px', textAlign: 'center' }}>
      <h1>{title}</h1>
      <p>Página ainda não implementada — chega numa próxima task da Sprint 4.</p>
    </main>
  )
}
